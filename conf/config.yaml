runmode: debug # 开发模式, debug, release, test 部署时环境变量覆盖
addr: :8080 # HTTP绑定端口
name: apiserver # API Server的名字
url: http://127.0.0.1:8080 # pingServer函数请求的API服务器的ip:port
max_ping_count: 10 # pingServer函数try的次数
jwt_secret: Rtg8BPKNEf2mB4mgvKONGPZZQSaJWNLijxR42qRgq0iBb5
log:
  writers: file,stdout # 部署时输出到 stdout
  logger_level: DEBUG
  logger_file: log/apiserver.log
  log_format_text: false
  rollingPolicy: size
  log_rotate_date: 1
  log_rotate_size: 1
  log_backup_count: 7
scene:
  username，真亏你能找到！你面前的就是银行最终的电子密码门，只要破解了这扇门，就能完成任务了！
  但是我们也不知道这个密码是多少位的，看来只能用全排列程序去破解了。
  （提示：实现一个全排列程序：输入一个整数n，然后输入n个不重复的数，返回所有数字的全排列。并将该程序文件上传至该路径）
